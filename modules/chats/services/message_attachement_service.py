from modules.chats.models import MessageAttachement

class MessageAttachementService:

    @staticmethod
    def create(*, request, serializer, data):
        ...

    @staticmethod
    def update(*, request, serializer, data, id):
        ...